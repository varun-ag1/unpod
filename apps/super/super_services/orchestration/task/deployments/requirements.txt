# ─── Deployment Requirements (call-task Docker image) ───────────────────────
# All 7 Prefect flows share this image.
# Core orchestration deps come from prefect_setup/requirements.txt.
# ─────────────────────────────────────────────────────────────────────────────

# Core orchestration (prefect, prefect-dask, prefect-docker, tabulate, psycopg2)
-r /prefect_setup/requirements.txt

# ─── Data / Models ──────────────────────────────────────────────────────────
pydantic>=2.0
python-dotenv

# ─── Database ───────────────────────────────────────────────────────────────
pymongo
redis
mongomantic @ git+https://github.com/parvbhullar/mongomantic.git@main

# ─── HTTP ───────────────────────────────────────────────────────────────────
httpx
requests

# ─── Web framework (libs/config.py imports at module level) ─────────────────
fastapi
starlette

# ─── Utilities ──────────────────────────────────────────────────────────────
phonenumbers
inflection
pyyaml
jinja2
python-Levenshtein
fuzzywuzzy
numpy
pandas
boto3
beautifulsoup4
rich

# ─── LiveKit API client (~42 MB, no agents/plugins/ML) ─────────────────────
# Used by CallDispatcher._execute_livekit() for lightweight room dispatch
livekit>=1.0.23

# ─── AI / LLM (Post-Call-Flow → PostCallWorkflow, lazy-loaded) ─────────────
openai
anthropic
litellm
tiktoken
dspy
langchain>=0.3.0
langchain-core
langchain-anthropic
langchain-openai
langgraph>=1.0.6

# ─── Eval Testing (Agent-Evals-Test → EvalTestAgent, lazy-loaded) ──────────
livekit-agents>=1.3.10

# ─── Pipecat (Execute-Task when AGENT_PROVIDER=pipecat, lazy-loaded) ───────
pipecat-ai[soundfile,aws,azure,cartesia,deepgram,elevenlabs,gladia,google,lmnt,ollama,runner,soniox]>=0.0.99
pipecat-ai-flows
pipecat-ai-whisker
