import CreatingIdentity from '../../../modules/auth/CreatingIdentity';

export const metadata = {
  title: 'Creating Identity | Unpod',
  description:
    'Creating your identity on Unpod to connect and collaborate with others.',
};

export default function PageCreatingIdentity() {
  return <CreatingIdentity />;
}
