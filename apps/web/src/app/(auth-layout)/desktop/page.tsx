import Dashboard from '../../../modules/Dashbaord';

export const metadata = {
  title: 'Sign In - Unpod',
  description: 'Access your Unpod account by signing in.',
};

export default function SignInPage() {
  return <Dashboard />;
}
