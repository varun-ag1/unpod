import Profile from '../../../../modules/Profile';

export const metadata = {
  title: 'User Profile | Unpod',
  description: 'Manage your Unpod account settings, preferences, and profile.',
};

export default function UserSettingsPage() {
  return <Profile />;
}
