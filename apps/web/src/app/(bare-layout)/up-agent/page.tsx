import UpAgent from '../../../modules/public/UpAgent';

export const metadata = {
  title: 'Agent | Unpod',
  description: 'View an Unpod AI agent.',
};

export default function UpAgentPage() {
  return <UpAgent />;
}
