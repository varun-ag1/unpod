# AWS Credentials for Desktop App Deployment
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git!

# AWS Credentials (Get from IAM Console)
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here
AWS_REGION=us-east-1

# S3 Bucket Configuration
S3_BUCKET=unpod-desktop-updates
S3_UPDATE_PATH=desktop-updates

# Update URL - Use ONE of the following options:
# Option 1: CloudFront (recommended for production)
S3_UPDATE_URL=https://your-cloudfront-id.cloudfront.net/desktop-updates

# Option 2: Direct S3 URL (for testing or if not using CloudFront)
# S3_UPDATE_URL=https://unpod-desktop-updates.s3.us-east-1.amazonaws.com/desktop-updates

# Optional: CloudFront URL (for deployment script display only)
CLOUDFRONT_URL=https://your-cloudfront-id.cloudfront.net
