type GetTabItemsParams = {
  agentType?: string;
  isNewAgent?: boolean;
  agentData?: any;
  updateAgentData?: (data: any) => void;
  headerForm?: any;
};

export const getTabItems = ({
  agentType,
  isNewAgent,
  agentData,
  updateAgentData,
  headerForm,
}: GetTabItemsParams) => {
  void agentType;
  void isNewAgent;
  void agentData;
  void updateAgentData;
  void headerForm;
  return [];
};
